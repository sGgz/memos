// MobileHeader disabled per requirements.

interface Props {
  className?: string;
  children?: React.ReactNode;
}

const MobileHeader = (_props: Props) => {
  return null;
};

export default MobileHeader;
